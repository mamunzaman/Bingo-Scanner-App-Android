# Next task

**Goal:** Device QA — full app i18n (EN/DE) after Phase 3 completion.

**Verify:** Settings → Deutsch → walk Home jackpot card, import/scan/OCR, manual entry, ticket detail, live sheets, profile/account snackbars/errors in German. Switch back to English and spot-check same flows. `./gradlew :app:assembleDebug` OK.

**Previous:** Phase 3 localization completion — remaining components + ViewModels (`strings_phase3.xml` + ~20 Kotlin files).
