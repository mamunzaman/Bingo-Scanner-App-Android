# Current priority

1. Device (once) — `historyPhotoImport` smoke: pending URI, GMS, gallery/uCrop, tab switch + discard, back
2. Optional — tune `MIN_VALID_CELLS_FOR_MANUAL_ENTRY_NAV` (20) after OCR validation
