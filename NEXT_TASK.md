# Next goal

- Rescan the same ticket as before; confirm `ImportTicketOcr` log shows a sane `finalRowMajor` (no duplicate-heavy junk) and counts match the UI.

# Verify

- Logcat: `ImportTicketOcr` (`finalRowMajor`, `displayedCount`, `distinctDisplayedCount`) and `ImportTicketFinalUi` after import.
