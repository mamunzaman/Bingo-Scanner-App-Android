package com.example.mamunbingoapp.ui.debug

object A11yDebug {
    // true only during audit, keep false by default
    const val ENABLED = false
    // visual overlay for touch targets (optional later)
    const val SHOW_TOUCH_TARGETS = false
}
